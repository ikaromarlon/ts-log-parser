{
  "name": "nodejs-logger-ohhevr",
  "version": "1.0.0",
  "description": "Develop the command line node.js application, which parses the input log file (see Input Example). The application should find all the log messages with the level error and print them into the output file (see Output Format).",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "./dist/parser.js",
  "scripts": {
    "start": "node ./dist/parser.js --input ./app.log --output ./errors.json",
    "dev": "ts-node ./src/parser.ts --input ./app.log --output ./errors.json",
    "lint": "ts-standard --fix",
    "test": "jest --runInBand --no-cache --silent",
    "build": "rm -rf ./dist && tsc"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.5.6",
    "git-commit-msg-linter": "^5.0.4",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "ts-standard": "^12.0.2",
    "typescript": "^5.2.2"
  },
  "ts-standard": {
    "env": "jest"
  },
  "dependencies": {
    "commander": "^11.0.0"
  }
}
